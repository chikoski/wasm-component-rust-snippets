export namespace ComponentFractionWithWitFraction {
  export { _new as new };
  declare function _new(numerator: number, denominator: number): Fraction;
}
export interface Fraction {
  numerator: number,
  denominator: number,
}
